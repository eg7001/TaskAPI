DB_NAME=taskapi
DB_USER=postgres
DB_PASSWORD=changeme
JWT_SECRET=changeme-use-a-long-random-string

# Optional: seeds one admin account on startup if both are set.
# Leave blank to skip seeding.
ADMIN_EMAIL=
ADMIN_PASSWORD=
